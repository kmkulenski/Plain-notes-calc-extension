# This static Chrome extension does not require environment variables.
# Keep this file as a placeholder for tooling consistency.
#
# Never place real credentials, tokens, cookies, account IDs, hostnames tied
# to credentials, or production connection strings in this repository.